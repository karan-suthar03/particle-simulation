{
  "name": "webgl-particle-simulation",
  "version": "1.0.0",
  "description": "WebGL2 Particle Simulation with Flow Fields",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "gh-pages": "^6.0.0"
  }
}